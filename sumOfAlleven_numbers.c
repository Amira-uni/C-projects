#include<stdio.h>
int main(){
int n;
sum = 0;
printf("Enter the number: ");
scanf("%d",&n);
if (n %2 = 0)
{
    for (int i = 0; i <=n; i= i+2)
    {
        sum = sum + i;
        printf("sum = %d", sum);
    }
    
}


    return 0;
}